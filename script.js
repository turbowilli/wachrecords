/* WACH RECORDS — site behavior + central content loader */
const menu=document.querySelector('.mobile-menu');const openBtn=document.querySelector('.nav-toggle');const closeBtn=document.querySelector('.mobile-menu .close');openBtn?.addEventListener('click',()=>menu?.classList.add('open'));closeBtn?.addEventListener('click',()=>menu?.classList.remove('open'));menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
const imagePath=name=>`assets/images/${name}`;const imagePair=name=>({jpg:imagePath(`${name}.jpg`),webp:imagePath(`${name}.webp`)});
function setPicture(picture,name,alt=''){if(!picture||!name)return;const pair=imagePair(name),source=picture.querySelector('source'),img=picture.querySelector('img');if(source)source.srcset=pair.webp;if(img){img.src=pair.jpg;img.alt=alt;}}
function setCaption(el,item){if(!el||!item?.number)return;const spans=el.querySelectorAll('span');if(spans[0])spans[0].textContent=`${item.number} — ${item.title||''}`;if(spans[1])spans[1].textContent=item.subtitle||'';}
function loadSiteChrome(){if(!window.siteContent)return;const s=siteContent;document.querySelectorAll('.brand').forEach(el=>{el.textContent=s.brand;});document.querySelectorAll('.desktop-nav').forEach(nav=>{nav.innerHTML=s.navigation.map(item=>`<a href="${item.href}">${item.label}</a>`).join('');});document.querySelectorAll('.mobile-menu').forEach(m=>{const close=m.querySelector('.close');m.innerHTML=(close?close.outerHTML:'<button class="close">Schließen</button>')+s.navigation.map(item=>`<a href="${item.href}">${item.label}</a>`).join('');});document.querySelectorAll('.site-footer').forEach(f=>{const copyright=f.querySelector(':scope > div:first-child');if(copyright)copyright.textContent=s.footer.copyright;const links=f.querySelector('.footer-links');if(links)links.innerHTML=`<a href="index.html">${s.footer.home}</a>`+s.navigation.map(item=>`<a href="${item.href}">${item.label}</a>`).join('')+`<a href="impressum.html">${s.footer.imprint}</a><a href="datenschutz.html">${s.footer.privacy}</a>`;});}
function loadHome(c){const hero=document.querySelector('.hero');if(hero){setPicture(hero.querySelector('picture'),c.hero.image,c.hero.title);hero.querySelector('h1').textContent=c.hero.title;hero.querySelector('p').textContent=c.hero.subtitle;}const intro=document.querySelector('.intro');if(intro){intro.querySelector('.kicker').textContent=c.introKicker;intro.querySelector('p').textContent=c.intro;}[...document.querySelectorAll('.gallery figure')].forEach((figure,i)=>{const item=c.gallery[i];if(item){setPicture(figure.querySelector('picture'),item.image,figure.querySelector('img')?.alt||'Fotografische Arbeit');setCaption(figure.querySelector('.caption'),item);}});[...document.querySelectorAll('.editorial-pair')].forEach((pair,pairIndex)=>{const start=pairIndex===0?5:9;[...pair.querySelectorAll('figure')].forEach((figure,j)=>{const item=c.gallery[start+j];if(item){setPicture(figure.querySelector('picture'),item.image,figure.querySelector('img')?.alt||'Fotografische Arbeit');setCaption(figure.querySelector('.caption'),item);}});});const quote=document.querySelector('.quote p');if(quote)quote.textContent=c.quote;const mag=c.magazine,magSection=document.querySelector('.magazine');if(magSection){magSection.querySelector('.section-kicker').textContent=mag.kicker;magSection.querySelector('h2').innerHTML=mag.title.replace('\n','<br>');magSection.querySelector('.reveal p').textContent=mag.text;magSection.querySelector('.meta').innerHTML=mag.meta.join('<br>');magSection.querySelector('.text-link').textContent=mag.linkText;magSection.querySelector('.mock-sub').textContent=mag.coverSub;}}
function loadWork(c){const grid=document.querySelector('#work-grid');if(!grid)return;grid.innerHTML=c.items.map(item=>`<figure class="work-card reveal"><picture><source srcset="${imagePath(`${item.image}.webp`)}" type="image/webp"><img src="${imagePath(`${item.image}.jpg`)}" alt="${item.alt||item.title||'Fotografische Arbeit'}" loading="lazy"></picture><figcaption class="caption"><span>${item.title||''}</span><span>${item.number||''}</span></figcaption></figure>`).join('');}
function loadPage(page,c){const hero=document.querySelector('.page-hero');if(hero){hero.querySelector('.section-kicker').textContent=c.kicker;hero.querySelector('.page-title').textContent=c.title;hero.querySelector('p').textContent=c.intro;}if(page==='magazine'){const preview=document.querySelector('.preview-copy');if(preview){preview.querySelector('.section-kicker').textContent=c.previewKicker;preview.querySelector('h2').textContent=c.previewTitle;preview.querySelector('p').textContent=c.previewText;}const textPage=document.querySelector('.text-page');if(textPage){const hs=textPage.querySelectorAll('h2'),ps=textPage.querySelectorAll('p');if(hs[0])hs[0].textContent=c.formatTitle;if(ps[0])ps[0].textContent=c.formatText;if(hs[1])hs[1].textContent=c.orderTitle;if(ps[1])ps[1].textContent=c.orderText;}const captions=document.querySelectorAll('.inside-grid figcaption');if(captions[0]?.querySelector('span'))captions[0].querySelector('span').textContent=c.preview1Caption;if(captions[1]?.querySelector('span'))captions[1].querySelector('span').textContent=c.preview2Caption;}if(page==='exhibitions'){const hs=document.querySelectorAll('.text-page h2'),ps=document.querySelectorAll('.text-page p');hs[0].textContent=c.currentYear;ps[0].textContent=c.currentText;hs[1].textContent=c.archiveTitle;ps[1].textContent=c.archiveText;}if(page==='about'){const sections=document.querySelectorAll('.text-page h2'),texts=document.querySelectorAll('.text-page h2 + p');c.sections.forEach((section,i)=>{if(sections[i])sections[i].textContent=section.title;if(texts[i])texts[i].textContent=section.text;});}if(page==='contact'){const block=document.querySelector('.contact-block');if(block)block.innerHTML=c.email.replace(/\n/g,'<br>');const note=document.querySelector('.text-page p');if(note)note.textContent=c.note;}if(page==='impressum'){const ps=document.querySelectorAll('.text-page p'),note=document.querySelector('.legal-note');if(note)note.innerHTML=`<strong>Hinweis:</strong> ${c.legalNote}`;if(ps[0])ps[0].innerHTML=c.provider.replace(/\n/g,'<br>');if(ps[1])ps[1].innerHTML=`${c.email}${c.phone?`<br>${c.phone}`:''}`;if(ps[2])ps[2].textContent=c.other||'Keine weiteren Angaben.';}if(page==='datenschutz'){const ps=document.querySelectorAll('.text-page p'),note=document.querySelector('.legal-note');if(note)note.innerHTML=c.privacyNote;if(ps[0])ps[0].innerHTML=`${c.privacyAddress}<br>${c.privacyEmail}`;if(ps[1])ps[1].textContent=c.hosting;if(ps[2])ps[2].textContent=c.privacyExternal;}}
function initContent(){if(!window.siteContent)return;loadSiteChrome();const path=location.pathname;if(path.endsWith('/index.html')||path.endsWith('/'))loadHome(homeContent);if(path.endsWith('/work.html'))loadWork(workContent);if(path.endsWith('/magazine.html'))loadPage('magazine',magazineContent);if(path.endsWith('/exhibitions.html'))loadPage('exhibitions',exhibitionsContent);if(path.endsWith('/about.html'))loadPage('about',aboutContent);if(path.endsWith('/contact.html'))loadPage('contact',contactContent);if(path.endsWith('/impressum.html'))loadPage('impressum',legalContent);if(path.endsWith('/datenschutz.html'))loadPage('datenschutz',legalContent);}
initContent();const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target);}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
